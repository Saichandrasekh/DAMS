{
  "mcpServers": {
    "sqlite": {
      "command": "node",
      "args": [
        "${CLAUDE_PROJECT_DIR:-.}/node_modules/mcp-server-sqlite-npx/dist/index.js",
        "${CLAUDE_PROJECT_DIR:-.}/backend/database/attendance.db"
      ]
    }
  }
}
