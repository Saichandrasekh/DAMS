{
  "mcpServers": {
    "sqlite": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-server-sqlite-npx",
        "${CLAUDE_PROJECT_DIR:-.}/backend/database/attendance.db"
      ]
    }
  }
}
